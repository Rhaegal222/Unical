/*

Un array contiene o celle vuote (es: valore 0) o celle che contengono una formica 
(es: valore 1). Associare il movimento di ogni formica ad un thread in modo che il 
comportamento delle formiche sia il seguente: Ogni formica inizialmente possiede una direzione (destra o sinistra) scelta in modo 
random. Quando una formica si scontra con un'altra, inverte la direzione del suo 
moto. Visualizzare il moto complessivo.

*/

